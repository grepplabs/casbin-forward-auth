apiVersion: v1
kind: Service
metadata:
  name: {{ include "casbin-traefik-forward-auth.fullname" . }}
  labels:
    {{- include "casbin-traefik-forward-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "casbin-traefik-forward-auth.selectorLabels" . | nindent 4 }}
